[package]
name        = "promptyst"
version     = "0.2.0"
entrypoint  = "lib.typ"
description = "Contract-first DSL for structured AI prompts. Core primitives + PROSE adapter layer. Deterministic Markdown output."
license     = "MIT"
authors     = []

keywords = ["prompt", "dsl", "ai", "contract", "structured", "markdown"]
